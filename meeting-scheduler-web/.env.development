# Config de desenvolvimento local (carregado automaticamente pelo `npm run dev`).
# API e frontend rodam em HTTP local para evitar bloqueio de mixed content.
VITE_API_BASE=http://localhost:5087
VITE_REDIRECT_URI=http://localhost:5173
